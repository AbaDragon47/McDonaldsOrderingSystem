#:kivy

<KVBL>:
#position widgets next to each other, use a horizontal layout
#else use vertical

orientation: 'vertical'

#can define buttons in box layout format with color, size, etc

Button:
    text:"btn1"
    background_color: 0,1,1,1
    font_size: 40
Button:
    text:"btn2"
    background_color: 0,1,0,1
    font_size: 40